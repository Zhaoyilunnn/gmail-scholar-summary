# GitHub Secrets 配置示例

# 必需 Secrets

## Gmail 凭证（base64 编码）
GMAIL_CREDENTIALS=base64_encoded_credentials_json
GMAIL_TOKEN=base64_encoded_token_json

## 邮件配置
RECIPIENT_EMAIL=your-email@example.com
GMAIL_LABEL=Scholar Alerts

## OpenAI 配置
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.openai.com/v1  # 可选，默认
OPENAI_MODEL=gpt-4o-mini  # 可选，默认

# 可选 Secrets（预留）

## Gemini 配置
GEMINI_API_KEY=xxxxxxxxxxxxxxxx

# 本地开发环境变量

DEBUG=1  # 启用调试日志
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=1000
FETCHER_TIMEOUT=30
